parameters:
  langauge: ''

steps:
- script: |
    $(Horton.FrameworkRoot)/scripts/tag-lkg.sh ${{ parameters.language }} vsts-${BUILD_BUILDID}
  displayName: "tag LKG ${{ parameters.language }}"
  condition: and(succeeded(), eq(variables['Horton.Commit'],'master'))
