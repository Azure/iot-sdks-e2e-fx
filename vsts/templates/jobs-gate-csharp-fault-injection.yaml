jobs:
- template: jobs-build-docker-image.yaml
  parameters:
    language: $(Horton.Language)
    repo: $(Horton.Repo)
    commit: $(Horton.Commit)
    forced_image: $(Horton.ForcedImage)

- job: "test"
  pool:
      vmImage: 'Ubuntu 16.04'
  dependsOn:
    - "build_linux_container"
  continueOnError: true
  strategy:
    matrix:
      edgehub_module_mqtt: { suite: edgehub_module_fi_mqtt }
      edgehub_module_mqtt_ws: { suite: edgehub_module_fi_mqtt_ws }
      edgehub_module_amqp: { suite: edgehub_module_fi_amqp }
      edgehub_module_amqp_ws: { suite: edgehub_module_fi_amqp_ws }

  steps:
  - template: steps-deploy-and-run-pytest.yaml
    parameters:
      language: $(Horton.Language)
      forced_image: $(Horton.ForcedImage)
      suite: $(suite)

